import React from "react";

// The disaster recovery and offline syncing engine runs silently in the background
// via OfflineSyncContext.jsx without rendering any simulation UI bar on the frontend.
export const DisasterResilienceBar = () => {
  return null;
};
